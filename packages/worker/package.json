{
  "name": "@perf-framework/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "run": "tsx src/cli.ts",
    "poll": "tsx src/poll-loop.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "playwright": "^1.49.0",
    "lighthouse": "^12.4.0",
    "chrome-launcher": "^1.1.2"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "tsx": "^4.19.0",
    "@types/node": "^22.0.0",
    "vitest": "^2.1.0"
  }
}
